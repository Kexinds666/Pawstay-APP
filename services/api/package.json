{
  "name": "api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/hello/local-dev.js",
    "build": "esbuild src/**/*.ts --bundle --platform=node --target=node20 --outdir=dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0",
    "@aws-sdk/lib-dynamodb": "^3.645.0",
    "@aws-sdk/client-s3": "^3.645.0",
    "@aws-sdk/s3-request-presigner": "^3.645.0",
    "@opensearch-project/opensearch": "^2.12.0",
    "stripe": "^16.7.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.6.3"
  }
}